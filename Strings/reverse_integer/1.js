/**
 * @param {number} x
 * @return {number}
 */

const x = 123;

var reverse = function (x) {
  if (x < -231 || x > 231 - 1) {
    return 0;
  }
  // return JSON.stringify(x);
};

console.log(reverse(x));
