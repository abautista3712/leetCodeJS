/**
 * @param {number} n
 * @return {number}
 */

const n = 3;

var climbStairs = function (n) {
  // Fibonacci Sequence
  // S(n) = S(n-1) + S(n-2)

  if (n == 1) {
    return 1;
  }
};

console.log(climbStairs(n));
