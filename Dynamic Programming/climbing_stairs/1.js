/**
 * @param {number} n
 * @return {number}
 */

const n = 3;

var climbStairs = function (n) {
  if (n == 1 || n == 2) {
    return n;
  }
  // Fibonacci Sequence
  // S(n) = S(n-1) + S(n-2)
};

console.log(climbStairs(n));
